int selectedColor = 1;

double currentValue = 6;
double sizeOfGrid = 6;

int totalGridSize =
    sizeOfGrid.toInt() * (sizeOfGrid.toInt() + sizeOfGrid.toInt());

var savedGrid = List<dynamic>.filled(totalGridSize, 6);

String nameOfSave = 'Test3';
